Arena Rubra – C2-STABLE-1-F9F-APK-M4c Stats & Export Foundation

Base:
- C2-STABLE-1-F9E1-APK-M4c HUD & Panel Usability Hotfix

Scope:
- Stats/export foundation only.
- No gameplay/AI/deck/map/tactic balance changes.

Checks performed:
- node --check src/*.js
- node --check data/*.js
- duplicate function scan
- duplicate HTML id scan
- F9F static checks:
  - BUILD_INFO updated to C2-STABLE-1-F9F-APK-M4c
  - state.matchStats present
  - initializeMatchStats() present
  - updateMatchStatsFromEvent() present
  - renderCurrentMatchStatsPanel() present
  - Stats panel buttons present
  - UI bindings for stats/export buttons present
  - emitGameEvent forwards typed events to matchStats
- zip integrity check

Manual smoke test recommended:
1. Splash -> Menu -> Setup -> Avvia partita.
2. Open Stats panel.
3. Verify current match stats section appears.
4. Move one unit and verify moves count changes.
5. Attack and verify attacks / damage metrics change.
6. Use a tactic and verify tactics count changes.
7. Test buttons:
   - Copia statistiche JSON
   - Copia report sintetico
   - Copia eventi JSON
   - Copia log completo
8. Finish or concede a match and verify final/winner fields appear in JSON/report.
