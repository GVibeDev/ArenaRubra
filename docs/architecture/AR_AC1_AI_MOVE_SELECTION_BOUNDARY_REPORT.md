# AR-AC1 — Advanced AI Move Selection Boundary Report

Date: 2026-09-06  
Baseline: `fef34ddf34fa20e111fbfe0d6721448b7e5bc6c5`  
Result: **PASS**

## Extracted boundary

Final Advanced F9T0 movement aggregation and selection now have one canonical owner in `src/ai/move_selection.js`.

The frozen `createAiMoveSelectionService()` API exposes exactly two operations:

- `botAdvancedMoveScoreF9T0(entry, context)`;
- `chooseAdvancedMove(unit, options, cachedStatus)`.

The service receives faction base scoring, optional Expert scoring, Strategic Status, movement-context construction and candidate scoring through five explicit late-bound ports. The legacy global names remain thin facades in `src/ai.js`.

## Preserved semantics

The service preserves the `0.85` emergency multiplier, `0.35` C2E3 multiplier, Expert zero fallback, candidate order, strict score equality, historical tie formula, first-candidate retention, coordinate reference identity and empty-list `null` result.

The selection port calls the global scoring facade late, retaining compatibility with runtime overrides. The first-`NaN` behaviour is intentionally unchanged rather than silently normalized.

## Exclusions

The service contains no DOM, browser storage, IndexedDB, global state, telemetry, RNG, rendering, sorting or movement action. It does not contain faction-specific base-scoring policy; that policy remains in `botFactionMoveBaseScoreF9T0` inside `src/ai.js`.

## Loading

The relevant classic-script order ends with:

```text
src/ai/strategic_status.js
  -> src/ai/move_context.js
  -> src/ai/move_selection.js
  -> src/ai.js
```

The runtime now contains 111 ordered scripts and the service performs no construction-time game read.

## Verification

| Gate | Result |
| --- | --- |
| Move-selection pre/post characterization | **PASS — 30/30** |
| Boundary/facade/loader contract | **PASS — 46/46** |
| Move-context characterization | **PASS — 47/47** |
| Move-context boundary contract | **PASS — 58/58** |
| Historical F9T0 compatibility smoke | **PASS — 38/38** |
| Complete Node smoke suite | **PASS — 146/146** |
| Complete Python/browser checks | **PASS — 69/69 in 347.6 seconds** |
| JavaScript syntax | **PASS — 260/260** |
| Starter 1.0 semantic manifest | **PASS — `eab4dadff4d9f6d4bc8e99bd38d7331e4d1559e69d40547b0039a75c1f140709`** |
| Map characterization hashes | **PASS — all five unchanged** |
| Golden Match hashes | **PASS — all five unchanged and deterministic** |
| `git diff --check` | **PASS** |

The browser gate used Python UTF-8 mode, Playwright 1.62.0 and system Chrome. Its temporary dependency directory and generated bytecode were removed; the three Control Center reference screenshots were restored from pre-run copies.

## Preserved Golden outputs

- `GOLDEN-001`: `07b533c8bf5fa42687ec7b617852995a325e352428f37c10156205a5a94031d6`;
- `GOLDEN-002`: `24a4a01759db83a561ffd79a9943eac3cf8828e19531fd3ff8f522b7c755e2f5`;
- `GOLDEN-003`: `11d215c48edd022bcc9a8bd8d4fc1c120017ec70b2e9c8f339adf7695eee1f34`;
- `GOLDEN-004-3P`: `493699236a59069d20f41a185bfcdacb8ed9a32ffb9a3388a34ae7484c88fee8`;
- `GOLDEN-005-4P`: `d899ba0fa2603f44422c5aebcec8206afd46907e8c32a5050b908e8907aa8ab5`.

## Rollback and review stop

Rollback is mechanical and migration-free: restore the two characterized implementations in `src/ai.js`, remove the service script and loader entry, update the VM harnesses, and rerun the same gates.

The seam is green and reversible. Faction-specific base scoring was subsequently characterized and extracted into `src/ai/faction_move_scoring.js`. Movement execution remains the next separate stateful action boundary.
