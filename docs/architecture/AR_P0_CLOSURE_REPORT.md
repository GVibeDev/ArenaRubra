# AR-P0 — Closure Report

Date: 2026-09-01  
Release target: Arena Rubra Starter 1.0 — Desktop / Web  
Comparison anchor: `fef34ddf34fa20e111fbfe0d6721448b7e5bc6c5`  
Result: **PASS — the technical baseline gate is closed; S2-C5a may start.**

This PASS does not rename the current candidate as `VALIDATA`. `BUILD_INFO`, the README and the project workflow still describe the visible build as a candidate. Product validation remains an explicit owner/workflow action distinct from this technical reconciliation.

## Closure decisions

- The owner authorized continuation when roadmap and repository analysis had no material contradiction. The three unavailable external documents are therefore waived for AR-P0 only; they were not reconstructed and do not silently override current code/data.
- `REFRACTOR_BASELINE` remains the single byte-exact comparison anchor above.
- The six ZIP-only ignored files remain quarantined in the hashed supplemental archive and are not imported, deleted or treated as runtime.
- Current code remains primary for catalog counts. Historical documentation remains evidence, not current authority.
- Tutorial Runtime 2.0, Match Data 2.0, the core rules, camera and other roadmap-frozen domains remain frozen except for confirmed defects.

## Files changed and reasons

- `tests/helpers/build_info_contract.js`: centralized the current build-shape and immutable logic-baseline contract.
- 25 historical Node smoke files: removed coupling to obsolete milestone version/name/channel strings while preserving their feature and behaviour assertions.
- `tests/browser_runtime.py` and 65 browser tests: made Chromium discovery portable across Linux and Windows/system Chrome, centralized build-version checks and retained the logic-baseline assertion.
- Browser fixtures were reconciled with already-published contracts: ten official runtime maps, Match Data 2.0 separate telemetry storage, all five published Challenges, permanent Mano/Fine turno controls, current Control Center labels and current Inspector sizing.
- `tests/requirements.txt`: pins the Python Playwright binding used by the gate.
- `css/style.css`: confirmed baseline layout bug fix only. The 420 px right Inspector overlapped the open hand by 11 px at 1440×960 and more in the 981–1120 px desktop band. The hand reserve is now 434 px (420 px Inspector + 14 px right inset). Mobile M4 is untouched.
- Five architecture/audit artifacts under `docs/architecture/`: baseline, dependency, globals, coverage and first-boundary evidence plus this closure report.

No gameplay, rules, balance, catalog, map, deck, mission, AI, storage schema, runtime JavaScript or asset file was intentionally changed.

## Tests executed

| Gate | Result | Notes |
| --- | --- | --- |
| JavaScript syntax | **PASS 208/208** | All `*.js` under `src/`, `data/` and `tests/`, including the new contract helper |
| Node smoke suite | **PASS 113/113** | No skip or expected-failure list |
| Python source/import surface | **PASS 68/68** | 67 executable scripts plus `browser_runtime.py`; all executable files completed in the final run |
| Python/browser suite | **PASS 67/67** | Final complete run: 357.3 seconds |
| Tutorial 5×5 aggregate | **PASS** | Five lessons, checkpoint resume, guidance and unified-result regression paths |
| Player lifecycle stability | **PASS** | Final full-suite run plus 10/10 isolated repetitions; async `ui.js` storage boot removed from the synthetic lifecycle fixture |
| Inspector/Hand layout | **PASS** | Desktop 1440×960 and mobile 820×480; no overlap after the CSS correction |
| Diff hygiene | **PASS** | `git diff --check`; generated screenshots restored and generated bytecode removed |

Browser environment used for this closure: Python 3.12.13, Playwright 1.62.0, system Google Chrome 151 on Windows. `browser_runtime.py` also resolves common Linux Chromium/Chrome paths and falls back to a Playwright-managed browser when available.

## Regressions explicitly sought

- core startup and precheck;
- 2P/3P/4P targeting and player lifecycle;
- ATT/DEF/HP, PS/Pressure, maps and victory flows exercised by the existing suite;
- missions, deck recovery, official decks and faction expansion;
- MatchRecord/MatchTelemetry separation and persistence access;
- Advanced/Expert integration tests already present;
- Tutorial lessons, checkpoint resume and Challenges;
- camera stability, renderer, hand/Inspector layout and mobile framing;
- Player/DEV profile, Control Center, themes and Snow map.

## Tests not executed / residual risks

- No physical-device, APK/AAB or Android wrapper validation; Android is outside Starter 1.0 scope.
- No owner manual visual/UX sign-off and no release-workflow validation; the build remains a candidate.
- No Golden Match set exists yet. This remains mandatory before extracting high-risk AI or central rule domains.
- GitHub Pages CI still performs only staging/existence checks; automated behavioural gates remain an AC1 exit condition.
- Required/optional/dev-only/legacy asset classification remains for S2-C6.
- Distribution currently hides DEV capabilities at runtime; it does not yet prove that DEV/Expert modules are absent from the public payload.

## Gate conclusion

AR-P0 is technically reproducible and green. Proceed to **S2-C5a — Content Freeze**. Do not begin an AR-AC1 extraction until the S2-C5a manifest and freeze decisions are recorded and its gate is PASS.
