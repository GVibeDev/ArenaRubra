# AR-AC1 — Faction Move Scoring Boundary Report

Date: 2026-09-07  
Baseline: `fef34ddf34fa20e111fbfe0d6721448b7e5bc6c5`  
Result: **PASS**

## Extracted boundary

The five Advanced F9T0 faction movement branches now have one canonical owner in `src/ai/faction_move_scoring.js`.

The frozen `createAiFactionMoveScoringService()` API exposes only `botFactionMoveBaseScoreF9T0(entry, context)`. Its geometry, battlefield, faction and commander collaborators cross 18 explicit read ports. `src/ai.js` retains the historical global name as a lazy thin facade.

## Ownership and exclusions

The service owns only faction base scoring and the common commander/danger tail. It does not own feature collection, final score aggregation, tie-breaking, coordinate selection or movement execution.

It contains no DOM, browser storage, IndexedDB, direct global state, telemetry, RNG, renderer or gameplay action. The two former `state.cells` reads now cross the `getCells` port only in the original Nexus-structure and Agathoi-builder branches.

## Loading

```text
src/ai/move_context.js
  -> src/ai/faction_move_scoring.js
  -> src/ai/move_selection.js
  -> src/ai.js
```

The runtime now contains 112 ordered scripts. The service performs no construction-time game read.

## Verification

| Gate | Result |
| --- | --- |
| Faction scorer pre/post characterization | **PASS — 14/14; 20-scenario hash unchanged** |
| Boundary/facade/loader contract | **PASS — 66/66** |
| Move-selection characterization | **PASS — 30/30** |
| Move-selection boundary contract | **PASS — 46/46** |
| Historical F9T0 compatibility smoke | **PASS — 38/38** |
| Complete Node smoke suite | **PASS — 148/148** |
| Complete Python/browser checks | **PASS — 69/69 in 349.1 seconds** |
| JavaScript syntax | **PASS — 263/263** |
| Starter 1.0 semantic manifest | **PASS — `eab4dadff4d9f6d4bc8e99bd38d7331e4d1559e69d40547b0039a75c1f140709`** |
| Map characterization hashes | **PASS — all five unchanged** |
| Golden Match hashes | **PASS — all five unchanged and deterministic** |
| `git diff --check` | **PASS** |

The browser gate used Python UTF-8 mode, Playwright 1.62.0 and system Chrome. Its isolated dependency directory and generated bytecode were removed. The three Control Center screenshots generated by the suite were restored exactly from `HEAD` without touching other working-tree changes.

## Rollback and review stop

Rollback is mechanical and migration-free: restore the characterized scorer in `src/ai.js`, remove the service script and loader entry, update VM harness routing, and rerun the same gates.

The read-only scorer seam is green and reversible. The adjacent `botMoveUnitF9T0` execution and `finishBotMove` finalization boundaries were subsequently characterized and extracted into `src/ai/move_execution.js`; broader attack, ability, purchase, tactic and turn orchestration remain out of scope until characterized independently.
