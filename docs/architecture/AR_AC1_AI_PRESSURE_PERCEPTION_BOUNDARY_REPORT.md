# AR-AC1 — Advanced AI Pressure Perception Boundary Report

Date: 2026-09-05  
Baseline: `fef34ddf34fa20e111fbfe0d6721448b7e5bc6c5`  
Result: **PASS**

## Extracted boundary

The first bounded Advanced AI slice now has one canonical owner in `src/ai/pressure_perception.js`.

The factory `createAiPressurePerceptionService(dependencies)` owns only:

- strategic-point counting and the historical three-PS missing-state fallback;
- projection of the canonical Pressure rules profile into the fields consumed by AI;
- proportional `requiredPs` fallback calculation;
- defensive cloning of the central coordinate;
- canonical central-control delegation and the historical center-cell fallback.

`strategicStatus`, finalization memory, faction maturity, garrison planning, scoring, movement, purchases, tactics and action execution remain in `src/ai.js`.

## Compatibility facade and loading

`src/ai.js` retains the three historical top-level functions:

- `botTotalPsCount`;
- `botPressureProfileF9T0`;
- `botControlsCentralF9T0`.

Each function delegates to one lazily constructed service. Existing Advanced and Expert callers, including the precheck that inspects `globalThis`, therefore keep the same names and call shape. The historically ignored argument accepted by calls such as `botPressureProfileF9T0(player)` remains harmless.

`index.html` loads `src/ai/pressure_perception.js` immediately before `src/ai.js` as an ordinary classic script. `state.js` and the Rules facade still load later. The injected callbacks resolve `state`, `pressureRuleProfile` and `playerControlsCentralStrategicPoint` at call time rather than at construction time. A dedicated contract constructs the singleton before Rules exists, installs the canonical queries afterward and proves that the existing instance observes them.

## Explicit read ports

The service receives providers for:

- current state and cells;
- canonical Pressure profile;
- semantic central-PS lookup;
- availability and invocation of the canonical central-control query;
- Pressure start, Pressure-win and maximum-round fallbacks.

The boundary is read-only. It has no DOM, storage, IndexedDB, direct `window`, telemetry, RNG, renderer, action or unit-mutation dependency. Its returned API is frozen.

## Compatibility audit

A parallel source/load-order audit found only two repository harnesses that execute the complete `src/ai.js` source directly; both now prepend the service source. Other tests either inspect unrelated source strings or install an explicit AI stub and need no migration. Runtime consumers continue through the three facades.

The audit also confirmed that the browser harnesses require ordinary unadorned script tags. No ESM, `defer`, loader split or caller migration was introduced.

## Verification

| Gate | Result |
| --- | --- |
| Focused pre/post characterization | **PASS — 14/14** |
| Boundary/loader/facade/late-binding contract | **PASS — 26/26** |
| Historical F9T0 compatibility smoke | **PASS — 36/36** |
| Complete Node smoke suite | **PASS — 135/135** |
| Complete executable Python/browser suite | **PASS — 69/69** |
| JavaScript syntax | **PASS — 242/242** |
| Starter 1.0 semantic manifest | **PASS — `eab4dadff4d9f6d4bc8e99bd38d7331e4d1559e69d40547b0039a75c1f140709`** |
| Map characterization hashes | **PASS — all five unchanged** |
| Golden Match hashes | **PASS — all five unchanged and deterministic** |
| `git diff --check` | **PASS** |

The complete browser gate ran in Python UTF-8 mode with Playwright 1.62.0 and system Chrome. Temporary Python dependencies, generated bytecode and test screenshots were removed or restored afterward.

## Preserved outputs

The five Golden Matches retain their frozen hashes:

- `GOLDEN-001`: `07b533c8bf5fa42687ec7b617852995a325e352428f37c10156205a5a94031d6`;
- `GOLDEN-002`: `24a4a01759db83a561ffd79a9943eac3cf8828e19531fd3ff8f522b7c755e2f5`;
- `GOLDEN-003`: `11d215c48edd022bcc9a8bd8d4fc1c120017ec70b2e9c8f339adf7695eee1f34`;
- `GOLDEN-004-3P`: `493699236a59069d20f41a185bfcdacb8ed9a32ffb9a3388a34ae7484c88fee8`;
- `GOLDEN-005-4P`: `d899ba0fa2603f44422c5aebcec8206afd46907e8c32a5050b908e8907aa8ab5`.

No AI choice, balance value, gameplay rule, state schema, catalog, persistence payload or build metadata changed.

## Rollback

Rollback is mechanical and migration-free: restore the three characterized implementations in `src/ai.js`, remove the service script and loader entry, restore the two direct-source harnesses, and rerun the same characterization, Node, browser, manifest and hash gates. No stored data migration is required.

## Review stop

This first AI seam is green and reversible. It does not authorize extraction of `strategicStatus`, scoring, memory, faction doctrine, action selection, Expert architecture, authoritative actions, CSS/HTML or the Distribution loader. Each later AI slice still requires its own local characterization before movement.
